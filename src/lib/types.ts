export interface ImageItem {
  url: string;
  alt?: string;
}

export interface Feature {
  key: string;
  label: string;
  value: string;
  icon: string;
}

export interface Facility {
  id: string;
  name: string;
  icon: string;
}
